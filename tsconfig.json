{
    "extends": "@tsconfig/svelte/tsconfig.json",

    "compilerOptions": {
        "types": [
            "@pyoner/svelte-types"
        ]
    },

    "include": ["src/**/*"],
    "exclude": ["node_modules/*", "dist/*"]
}